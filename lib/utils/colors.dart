import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF00FF88);
  static const background = Color(0xFF0B1F3A);
  static const card = Color(0xFF122B4A);
  static const text = Colors.white;
  static const muted = Colors.grey;
}
